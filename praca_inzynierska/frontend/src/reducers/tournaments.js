const initialState = {
  isLoading: true,
  incomingTournaments: [],
  historyTournaments: [],
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
